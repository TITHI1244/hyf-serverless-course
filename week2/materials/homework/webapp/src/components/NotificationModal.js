import React from 'react'

export default function NotoficationModal({text}) {
    return (
        <div className="notification-modal">
            <h1>{text}</h1>            
        </div>
    )
}
